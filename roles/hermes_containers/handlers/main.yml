- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when: not ansible_check_mode

- name: Restart excalibur hermes
  ansible.builtin.service:
    name: excalibur-hermes
    state: restarted
  when:
    - excalibur_hermes_enable_service | bool
    - not ansible_check_mode
